[
  {
    "name": "vibeue_status",
    "description": "Check whether Unreal Engine is running and the VibeUE MCP server is reachable. Returns setup status and next steps if configuration is missing. Call this first when tools are failing or to verify your environment before starting work.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  }
]
